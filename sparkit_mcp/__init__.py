"""SPARKIT MCP server — exposes the SPARKIT scientific research agent as an
MCP toolset for Claude Desktop, Cursor, and other MCP clients.

See README.md for installation and configuration. The package's entry
point is `sparkit_mcp.server:main`, registered as the `sparkit-mcp`
console script."""

__version__ = "0.1.0"
